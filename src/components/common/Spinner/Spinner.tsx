import React from 'react';
import { ActivityIndicator } from 'react-native';

const Spinner = () => {
  return (
    <ActivityIndicator
      size="small"
      color="#fff"
    />
  )
}

export default Spinner